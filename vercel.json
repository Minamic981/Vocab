{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && mkdir -p backend/static && cp -r frontend/dist/* backend/static/",
  "installCommand": "cd backend && npm install",
  "functions": {
    "backend/app.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/backend/app.js"
    }
  ]
}
